apiVersion: v1
kind: Service
metadata:
  name: myservice
spec:
  type: LoadBalancer
  selector:
    app: tetrisgame   
  ports:
    - port: 8080      
      targetPort: 3000   
      nodePort: 31767  
